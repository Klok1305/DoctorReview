# Постоянный контекст проекта

Эти инструкции действуют для всего репозитория.

## Самое важное при релизе

После успешного push нового тега **сразу сообщи «Релиз <version> запущен» и всегда дай [ссылку на страницу релизов](https://github.com/Klok1305/DoctorReview/releases)**. Не жди завершения GitHub Actions, сборки установщиков или публикации файлов. Не запускай мониторинг без отдельной просьбы. При ошибке push сообщи ошибку и ту же ссылку, не объявляя релиз запущенным. Подробный порядок подготовки — в разделе «Запуск релиза» ниже.

## Начало каждой задачи

1. Сначала полностью прочитай [`docs/PROJECT_MAP.md`](docs/PROJECT_MAP.md).
2. Используй карту как индекс: открывай только файлы, относящиеся к задаче, и связанные тесты. Не начинай с полного обхода репозитория, `node_modules`, `dist`, `tmp`, `output` или с чтения сгенерированного `index.html`.
3. Перед изменением всё равно проверь актуальный код в перечисленных картой файлах. Карта ускоряет навигацию, но код остаётся источником истины.
4. Если карта расходится с кодом, исправь карту в той же задаче.

## Постоянный TODO по архитектуре

Источник: [docs/ARCHITECTURE_REVIEW.md](docs/ARCHITECTURE_REVIEW.md), обзор от 2026-09-12. Пользователь поручил выполнять этот список и сохранять прогресс между сессиями. `[x]` ставить только после реализации и необходимых проверок; частично выполненный пункт оставлять `[ ]` с точным остатком. Не повторять завершённый аудит и готовые этапы. Перед окончанием работы или исчерпанием лимита обновить «Точку продолжения» ниже: изменения, проверки, незаконченная работа и следующий конкретный шаг. Не запускать новый релиз без отдельной просьбы.

**Статус тикета: открыт.** Когда все A01–A12 выполнены и проверены, явно сообщить пользователю, что весь список завершён, перечислить итоговые проверки и поменять этот статус на **«закрыт»**. Не закрывать тикет при частичном выполнении, исчерпании лимитов или наличии блокирующих вопросов; сохранять следующий шаг для продолжения.

- [x] **A01 / P1 — атомарный импорт (§1).** Аналитика и происхождение фиксируются одной транзакцией через `database:save-import`; отказ откатывает файл и останавливает пачку. Проверены отказ после записи источника, повтор ZIP, отложенное автосохранение, порядок/ошибки очереди, миграция старой SQLite и снимки 1–4.
- [x] **A02 / P1 — сопоставимые сводные KPI (§2).** Чек, частота, расписание, собственная запись, курсовое, первичка, конверсия и доли требуют сопоставимых данных всех включённых врачей; неполные значения — `null` с `coverage` и пояснением UI. Проверены полные/неполные числовые результаты, разные окна, ID/ФИО и пересечение групп; отчёт отделения использует тот же агрегат.
- [x] **A03 / P2 — точечное сохранение (§3).** Команды с `dataRevision` записывают только изменённые настройки/заметки/месяцы/врачей, импорт сохраняет порцию вместе с источником; загрузка месяцев идёт порциями. Полные снимки и старые SQLite/JSON читаются.
- [x] **A04 / P2 — расчётные кэши (§4).** Ограниченные LRU для скалярных сводок, пациентских деталей, В4 и отделений; выборочная инвалидация. Синтетический набор 10 × 12 × 250 сравнен с прежним кодом на тех же данных; результаты и оговорка о холодной сводке отделения — в точке продолжения.
- [x] **A05 / P2 — полнота мобильных оценок (§5).** Публикация/валидатор/PWA передают и показывают coverage/preliminary/missing, незаданная цель не становится нулём; старые публикации читаются.
- [x] **A06 / P2 — определения В4 (§6).** Новое разбиение Admin/Viewer явно помечено `partition-v1-36m`, прежние KPI/балл и мобильные пересекающиеся группы — `legacy-overlap-v1`; PWA поясняет различие. Формулы и старые пакеты сохранены.
- [x] **A07 / P2 — календарная динамика (§7).** Полностью отсутствующие месяцы остаются пропусками в Admin, отделении, карточках и PWA; предыдущий календарный месяц и окна средних определены явно. Проверен январь–март без февраля.
- [ ] **A08 / P2 — фоновые операции (§8).** Есть прогресс/отмена импорта между файлами, уступка renderer event loop и async scrypt с пределом конкуренции 2 на сервере и при упаковке мобильного пакета. Осталось: worker и ограниченные очереди для синхронного XLSX-парсинга, SQLite-записи и Viewer/мобильной упаковки; прогресс/отмена этих задач и замеры задержки событий. Параметры шифрования не ослаблять.
- [x] **A09 / P2 — независимый экспорт (§9), без удаления по решению пользователя.** Неизменяемая ReportModel v1 с SHA-256-ревизией и HTML/PDF/JSON-адаптерами; общие зашифрованные страницы и выдача ключей вместо копий; предварительная оценка размера до KDF/упаковки; атомарное поколение каталога Viewer. ZIP 2/3 и прежний корневой каталог читаются, HTML 2/3 самодостаточны. Автоочистка старых выпусков и поколений явно исключена пользователем и не реализована.
- [ ] **A10 / P2 — устойчивость PWA и мобильное развитие (§10).** Таймауты/отмена/повтор, идемпотентные порции и resume, числовой контракт отчёта и выдача нужного периода реализованы. Осталось проверить слабую сеть и реальные телефоны/WebKit. Offline-отчёты, мобильный импорт и совместное редактирование — отдельные этапы после ядра/ReportModel с явной политикой хранения и конфликтов.
- [x] **A11 / P2 — предел мобильных сессий (§11).** Очистка использует настоящее время; при лимите 2000 новый вход получает HTTP 503/Retry-After. Проверены сохранность активных сессий и загрузок, замена только своей сессии, освобождение просроченной сессии и новый вход после перезапуска с сохранённым пакетом. Хранилище остаётся в памяти одного процесса.
- [ ] **A12 / P2 — регрессии, нагрузка и CI (§12).** Регрессии A01–A07 (числовые для расчётов, структурные для форматов), HTTP-регрессии A10/A11, benchmark времени/памяти и smoke/PDF в CI добавлены. Осталось: общие эталонные результаты Admin/Viewer/PWA для одного набора, регрессии A08, воспроизводимые сетевые сценарии и проверка CI на GitHub. Статические проверки строк не считать проверкой числового результата.

### Точка продолжения

- Обновлено: **2026-09-24**. Версия в `package.json` — **2.6.21**. Завершённые архитектурные пункты включены в выпуск; оставшиеся пункты перечислены ниже.
- Завершены и проверены **A01–A07, A09, A11**. Открыты **A08, A10, A12**; статус тикета остаётся **«открыт»**. Причину A01 повторно не исследовать: прежняя падавшая регрессия проходит.
- A03: `saveMutation` с `dataRevision`, точечные команды IPC/preload, атомарные порции импорта, выборочная загрузка месяцев по 24; старые SQLite/JSON и полный снимок сохранены. A04: LRU-кэши скаляров/деталей/отделения и выборочная инвалидация. На одинаковом синтетическом наборе 10 врачей × 12 месяцев × 250 пациентов в каждом из 3 окон старый код: холодные персональные расчёты **1492 мс**, рост heap **149 МБ**, холодное отделение **76 мс**, тёплое **47 мс**; новый: **709 мс**, **48 МБ**, **280 мс**, **0,04 мс** соответственно. Холодная сводка отделения стала медленнее из-за построения деталей по запросу; суммарное первое вычисление персональных метрик и отделений быстрее. Это локальный Node 24, не измерение реальной клиники.
- A05: мобильная публикация, серверный валидатор и PWA сохраняют и показывают полноту/предварительность/пропуски и отличают незаданную цель от нуля; старый формат читается. A06: Admin/Viewer `partition-v1-36m`, старые KPI/баллы и PWA `legacy-overlap-v1` с явным пояснением, формулы не изменены. A07: январь–март без февраля проверен численно в Admin, отделении, карточках и мобильном отчёте.
- Частичный A08: импорт показывает прогресс и отменяется после текущего файла, renderer уступает событиям между файлами; серверный scrypt асинхронный с конкуренцией 2, упаковка мобильного пакета также использует async scrypt с пределом 2. XLSX, SQLite и упаковка Viewer по-прежнему выполняют синхронную тяжёлую работу; worker/ограниченные очереди и замеры задержки событий остаются.
- Частичный A10: API таймауты, отмена и повторы, возобновляемая загрузка файла с SHA-256 и идемпотентными порциями, повторяемое завершение, числовой контракт v1 и серверная выдача одного периода. Слабая сеть на устройстве и реальные телефоны/WebKit пока не проверены.
- Частичный A12: 175/175 Node-тестов, benchmark прошёл, `audit:repo` прошёл, Electron smoke и PDF smoke прошли вне ограниченной среды; CI/release workflow теперь запускают benchmark/smoke/PDF, но сам GitHub CI ещё не запускался на этих изменениях. Тесты с временными каталогами Viewer под `%TEMP%` иногда получают случайный `EPERM` при rename; с `$env:TEMP=$env:TMP=(Resolve-Path tmp).Path` полный набор прошёл. Общий эталон чисел Admin/Viewer/PWA и реальные сетевые сценарии ещё нужны.
- A01: `saveSnapshot(snapshot, importRecords)` → одна транзакция; новый IPC/preload `saveImport`; очередь не объединяет атомарные команды с обычными снимками. `withImportMutation` откладывает обычное сохранение до commit/отката. Импорт фиксируется по файлу, ошибки откатываются, архив подтверждается с последним файлом, повтор проверяет фактические успешные источники SQLite.
- A02: `aggregateDeptMonth` возвращает полноту каждого отношения; неполные отношения не делятся, расписание не подменяется средним процентов, разные окна не смешиваются. `aggregateCoverageHtml` поясняет неполноту; отчёт отделения использует точный список врачей и общую дедупликацию. Методика ID/ФИО и пересекающихся отношений пациент–врач описана в карте.
- A11: отказ при лимите вместо искусственной очистки будущим временем; HTTP-регрессия проверяет лимит, активные загрузки, повтор своего входа, освобождение места и перезапуск. Новые пакеты по-прежнему отзывают старые сессии.
- Выполнена отдельная просьба по интерфейсу: одна таблица четырёх групп в прежнем оформлении, по полю визитов и периода на строку; таблица пяти групп убрана. Старые параметры B–F сохранены без изменений. Пересобран `index.html`, обновлена карта.
- Основа A06 была заложена ранее: новые HTML/ZIP Viewer сохраняют тот же В4 Admin с четырьмя непересекающимися группами за 36 месяцев, диаграммой и порогами; защищённый реестр пациентов строится из той же базы. Старые публикации остаются самодостаточными. Текущее явное обозначение разных методик завершило пункт без замены формул KPI/балла.
- A09: Admin фиксирует выбранные страницы в `klinvekt-report-model` v1 с SHA-256-ревизией; Viewer ZIP/HTML v4 хранит каждую страницу один раз и выдаёт получателям зашифрованные ключи. Размер оценивается до массового KDF/упаковки. Установленный Viewer собирает полное поколение метаданных и атомарно меняет `_viewer/current.json`; отказ переключения оставляет активной прежнюю публикацию. ZIP v3 и старый корневой каталог проверены регрессией. Пресет «Выбрать всё» и постоянные PIN сохранены. По прямому указанию пользователя автоудаление прежних выпусков, общих страниц и поколений не добавлено.
- Исправление экспорта Viewer: `build/viewer-html-sanitizer.js` теперь очищает HTML в renderer **до** вычисления ID и ревизии страниц, а сервис повторно проверяет его тем же модулем. Причина ошибки — `onclick` в ссылке «Настройки» на дашборде: сервис удалял его после хеширования. Добавлена регрессия реальной модели renderer и проверка отказа при подмене страницы. `index.html` пересобран, изменён состав сборки Admin и обновлена карта.
- В диалоге Viewer добавлены пояснение, что новый файл содержит лишь выбранные месяцы, и кнопка «Выгрузить всё в один HTML»: она применяет полный пресет и сразу начинает экспорт с действующим администраторским PIN. Прежняя кнопка «Выбрать всё» оставлена для ручной проверки выбора или ZIP.
- Проверки релиза 2.6.17: `pnpm test` — **164/164**; `pnpm run audit:repo` — passed; `pnpm run test:smoke` и `pnpm run test:pdf` вне ограниченной среды — **passed**, ошибок renderer нет; `pnpm run dist:all` собрал установщики Admin и Viewer; `pnpm run build:mobile-server` собрал серверный каталог. Данные проверок только синтетические.
- В версии 2.6.18 ускорена подготовка снимков Viewer: `cloneDashboardSnapshot` для HTML/ZIP не ожидает четыре кадра и загрузку клонированных изображений, но по-прежнему принудительно обновляет Chart.js перед сохранением canvas; PDF оставляет прежний режим ожидания. Синтетический скрытый Electron показал **3,23 с → 0,36 с** на одной личной странице; это замер ограниченного сценария, не оценка полного экспорта на реальных данных. Проверены 11 декодируемых графиков Viewer. Проверки кода: `pnpm test` **164/164**, smoke и PDF smoke **passed**, аудит **passed**. `pnpm run dist:all` собрал оба установщика, `pnpm run build:mobile-server` — серверный каталог.
- Для 2.6.19 по предоставленным JSON-копиям и текущей SQLite подтверждено, что список активных получателей Viewer состоит из заведующих, а врачей с личной «Выработкой» больше. Старая полная кнопка выбирала лишь активных, хотя остальные были вложены в HTML как отчёты отделений. Новая кнопка берёт всех врачей с личной выработкой и включённых заведующих с отчётами отделений, не меняет флаги доступа/PIN; main проверяет полноту отчётов и разрешает неактивных получателей только для полной HTML-публикации. Ручная HTML/ZIP-выгрузка по-прежнему ограничена активными. Регрессии проверяют выбор и вход неактивного врача с сохранённым PIN в новом HTML. Проверки: `pnpm test` **166/166**, smoke и PDF smoke **passed**, аудит **passed**, `dist:all` собрал оба установщика, `build:mobile-server` собрал серверный каталог. Рабочие JSON, HTML и PIN не коммитить.
- В 2.6.20 личный В3 новых HTML/ZIP сохраняет исходный снимок Admin выбранного окна назначений вместо упрощённого Viewer-переключателя. Вложенные строки 1С раскрываются в обоих Viewer без inline-скриптов; старые публикации и их переключатели продолжают открываться. Печать Admin/Viewer и Chromium PDF использует альбомный A4, перенос строк таблиц, отдельные страницы векторов и целую карточку целей. Синтетический smoke сравнил каждую строку таблицы Admin и HTML; PDF smoke прошёл, 11 страниц визуально просмотрены. `pnpm test` **167/167**, аудит **passed**, `dist:all` собрал оба установщика, `build:mobile-server` — серверный каталог. Форматы, PIN и SQLite не изменены.
- Следующий конкретный шаг — **A08**: выделить синхронный XLSX-парсинг в worker с одной ограниченной очередью и проверкой отмены/задержки событий, затем аналогично вынести SQLite-запись и упаковку Viewer. После этого A10: проверить возобновление на искусственно медленной/обрывающейся сети и WebKit/телефонах; A12: общий числовой эталон Admin/Viewer/PWA и результат CI. Не закрывать тикет до этих проверок.
- Окружение: для pnpm использовать `$env:pnpm_config_verify_deps_before_run = 'false'`, если автоматическая проверка пытается переустановить зависимости без TTY. Electron smoke внутри ограниченной среды в последнем прогоне завершился системной ошибкой запуска Chromium GPU; вне неё smoke и PDF проходят. Данные для тестов только синтетические.

## Обязательное сопровождение карты

Обновляй `docs/PROJECT_MAP.md` в той же задаче, если изменение затрагивает хотя бы один из пунктов:

- добавлен, удалён или переименован модуль, сервис, точка входа либо основной каталог;
- изменились владельцы логики, поток данных или связь Admin → IPC → сервисы → SQLite;
- изменились схема/миграции SQLite, формат снимка состояния, импорта, резервной копии, PDF, Viewer ZIP или автономного HTML;
- изменились права доступа, PIN, шифрование, обработка персональных данных или границы между Admin и Viewer;
- изменились сборка, генерация `index.html`, команды проверки, CI, релизный процесс или состав установщиков;
- появилась крупная пользовательская подсистема, для которой будущему агенту нужна новая точка навигации.

Не превращай карту в changelog. Локальный рефакторинг или исправление внутри уже описанной подсистемы не требует записи, если утверждения и маршруты карты остались верны. При обновлении карты меняй дату проверки; номер версии бери только из `package.json`.

## Неподвижные правила разработки

- `index.html` генерируется из `build/index.template.html`, `build/app.css` и `build/app-*.js` командой `pnpm run assemble`. Не исправляй его вручную; после правок исходников пересобери и включи обновлённый `index.html` в изменение.
- Не добавляй в Git данные клиники, выгрузки, базы, резервные копии, отчёты, установщики или секреты. После изменения состава исходников обнови одновременно `.gitignore` и `scripts/audit-repository.ps1`, затем запусти `pnpm run audit:repo`.
- Для изменений SQLite, Viewer, экспорта и доступа считай совместимость старых рабочих баз и уже выпущенных пакетов обязательной, пока пользователь явно не разрешил разрыв.
- Минимальная проверка кода — `pnpm test`; для UI/рендеринга используй `pnpm run test:smoke`, для PDF — `pnpm run test:pdf`. Полная релизная последовательность описана в карте.

## Запуск релиза

- По просьбе выпустить релиз подготовь проверенные изменения, обнови версию в `package.json`, карту и релизную документацию. Закоммить изменения и отправь `main` вместе с новым тегом `v<version>` через `git push --atomic origin main v<version>`.
- После успешной отправки тега сразу сообщи «Релиз <version> запущен» и дай ссылку https://github.com/Klok1305/DoctorReview/releases. Не жди завершения GitHub Actions, сборки установщиков или публикации артефактов. Не запускай мониторинг без отдельной просьбы.
- Успешный push означает запуск, а не готовность релиза. Если push не удался, сообщи об ошибке вместо заявления о запуске и всё равно дай ссылку на страницу релизов. Действующие теги и опубликованные релизы не перезаписывай.
- Проверки изменённого кода выполняются до push; уже успешно выполненные проверки не повторяй без новых изменений или выявленных проблем.
